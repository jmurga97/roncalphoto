{
  "name": "@roncal/email-worker",
  "description": "RoncalPhoto transactional email worker",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "wrangler dev --port 8788 --inspector-port 9231",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@roncal/email-templates": "workspace:*",
    "hono": "^4.11.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260502.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.60.0"
  }
}
